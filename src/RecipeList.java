import java.io.IOException;
import java.util.ArrayList;
import java.util.Set;
import java.util.HashSet;

public class RecipeList {

   /*

    public Set<String> makeRecipeLinks (RecipeParser parser) throws IOException {

        return parser.parseBaseRecipeURL();
    }
    public void printRecipeLinks() {
        System.out.println("\n\n\nDone.");
        System.out.printf("URL Parsed: %s \n \n", baseUrl);

        for (String currentUrl : baseRecipeLinks) {
            System.out.println(currentUrl);
        }

        System.out.printf("\n %d Recipes found.", baseRecipeLinks.size());
    }
    private String baseUrl;
    private Set<String> baseRecipeLinks;

    public RecipeList (String baseUrl) throws IOException {

        this.baseUrl = baseUrl;
        RecipeParser parser = new RecipeParser(baseUrl);
        this.baseRecipeLinks = makeRecipeLinks(parser);
        printRecipeLinks();

    }
    */













}
